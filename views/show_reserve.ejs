<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ค้นหาข้อมูลการจอง</title>
    <link rel="stylesheet" href="show_reserve.css">
</head>
<body>
    <main> 
        <!--NAV BAR-->
        <header>
            <nav>
                <img class="logo" src="images/LOGO.png" alt="LOGO">
                <div class="nev_links">
                    <a href="/index">หน้าแรก</a>
                    <a href="/gallery">คลังภาพ</a>
                    <a href="/travel">สถานที่ท่องเที่ยวใกล้เคียง</a>
                    <a href="/contact">ติดต่อเรา</a>
                </div>
                <div>
                    <a class="check" href="/check_reserve">ตรวจสอบการจอง</a>
                </div>
            </nav>
        </header>
        <!--Content-->
       <section class="success_reserve">
                <img class="IMG" src="images/book-svgrepo-com.svg" alt="">
                <div class="TEXT">
                <p>ข้อมูลการจองของท่าน</p>
                <% customer_data.forEach(function(item) { %>
                    <p>รหัสการจองของคุณคือ : <%= item.reserve_id  %></p>
                    <p>เลขที่ห้องที่จอง : <%= item.customer_name  %></p>
                    <p class="name">ชื่อ : <%= item.customer_name  %></p>
                    <p class="name">นามสกุล : <%= item.customer_lastmane  %></p>
                <% }) %>
                <% checkin.forEach(function(item) { %>
                    <p>วันที่เข้าพัก : <%= item.checkin  %></p>
                <% }) %>
                <% checkout.forEach(function(item) { %>
                    <p>วันออกที่พัก : <%= item.checkout  %></p>
                <% }) %>
                </div>
       </section>
    </main>
</body>
</html>